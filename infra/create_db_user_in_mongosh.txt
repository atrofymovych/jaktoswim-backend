// See available databases
show dbs

// Switch to the org's database (it will be created on first write)
use housemaster

// Optional: create a tiny doc so the DB exists immediately
db.init.insertOne({ createdAt: new Date() })

// Create an application user scoped to this database
db.createUser({
  user: "test",
  pwd: "test",
  roles: [
    { role: "readWrite", db: "housemaster" }
  ]
})

// Verify user
show users
