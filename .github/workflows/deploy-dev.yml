# .github/workflows/deploy-dev.yml

name: build-and-deploy-dev

on:
  push:
    branches: [dev]
  workflow_dispatch:

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run all tests with verbose output
        run: npm run test:all
        env:
          NODE_ENV: test
          # Test environment variables (these should be set as GitHub secrets if needed)
          CLERK_SECRET_KEY: test-secret-key
          RESEND_API_KEY: test-resend-key
          TWILIO_ACCOUNT_SID: test-account-sid
          TWILIO_AUTH_TOKEN: test-auth-token
          MONGO_DB_CLUSTER: test
          MONGO_DB_APP_NAME: test

      - name: Run test coverage
        run: npm run test:coverage

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage/lcov.info
          flags: unittests
          name: codecov-umbola-dev
          fail_ci_if_error: false

  build_and_push:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Set image name
        run: echo "IMAGE=${{ secrets.DOCKERHUB_USERNAME }}/dao" >> $GITHUB_ENV

      - name: Build with Compose
        env:
          IMAGE: ${{ env.IMAGE }}
        run: docker compose -f docker-compose.build.yml build

      - name: Push :latest
        env:
          IMAGE: ${{ env.IMAGE }}
        run: docker compose -f docker-compose.build.yml push

      - name: Tag & push :sha
        env:
          IMAGE: ${{ env.IMAGE }}
        run: |
          docker tag ${IMAGE}:latest ${IMAGE}:${GITHUB_SHA}
          docker push ${IMAGE}:${GITHUB_SHA}

      - name: Tag & push :DEV
        env:
          IMAGE: ${{ env.IMAGE }}
        run: |
          docker tag ${IMAGE}:latest ${IMAGE}:DEV
          docker push ${IMAGE}:DEV

  deploy:
    needs: [test, build_and_push]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/dev'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Copy docker-compose.dev.yml to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DEV_SSH_HOST }}
          username: ${{ secrets.DEV_SSH_USERNAME }}
          key: ${{ secrets.DEV_SSH_PRIVATE_KEY }}
          source: 'docker-compose.dev.yml'
          target: '~/dao-app-dev'

      - name: Copy prometheus.yml to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DEV_SSH_HOST }}
          username: ${{ secrets.DEV_SSH_USERNAME }}
          key: ${{ secrets.DEV_SSH_PRIVATE_KEY }}
          source: 'prometheus.yml'
          target: '~/dao-app-dev'

      - name: Copy promtail-config.yml to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DEV_SSH_HOST }}
          username: ${{ secrets.DEV_SSH_USERNAME }}
          key: ${{ secrets.DEV_SSH_PRIVATE_KEY }}
          source: 'promtail-config.yml'
          target: '~/dao-app-dev'

      - name: Deploy application
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEV_SSH_HOST }}
          username: ${{ secrets.DEV_SSH_USERNAME }}
          key: ${{ secrets.DEV_SSH_PRIVATE_KEY }}
          script: |
            cd ~/dao-app-dev
            echo "${{ secrets.DEV_DOT_ENV }}" > .env
            export IMAGE=${{ secrets.DOCKERHUB_USERNAME }}/dao:DEV
            docker compose -f docker-compose.dev.yml pull
            docker compose -f docker-compose.dev.yml up -d --force-recreate
